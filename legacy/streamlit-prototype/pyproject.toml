[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mini-project-2"
version = "0.1.0"
description = "Initial sleep consultation chatbot prototype (historical source)"